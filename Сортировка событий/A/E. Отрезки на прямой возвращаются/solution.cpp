#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <stdint.h>

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    int n, k;
    std::cin >> n >> k;
    
    return 0;
}
